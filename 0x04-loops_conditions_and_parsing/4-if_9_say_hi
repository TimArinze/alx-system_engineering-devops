#!/usr/bin/env bash
# A bash script that displays Best School 10 times, 9th iteration is Hi
i=0
while ((i < 10))
do
	if ((i == 8)); then
		echo "Hi"
	elif ((i != 8)); then
		echo "Best School"
	fi
	((i++))
done
